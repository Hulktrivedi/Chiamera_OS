# Define sizes (in MB) for soft partitions
# SYSTEM partition is fixed internally; only these user partitions are configurable

# Downloads folder partition (default 1024MB)
DOWNLOAD_SIZE_MB=1024

# Secure Vault folder partition (default 2048MB)
VAULT_SIZE_MB=2048

# 📝 Future: Add MEDIA_SIZE_MB or APPDATA_SIZE_MB if needed
